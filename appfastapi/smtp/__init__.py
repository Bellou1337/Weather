from .smtp import SMTPSender

# from appfastapi.config import config
# smtp_sender = SMTPSender(config["SMTP"]["server"], config.get("SMTP", "port"), config["SMTP"]["email"], config["SMTP"]["password"])